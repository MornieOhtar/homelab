apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: pg-sonarr-data
spec:
  instances: 2
  imageCatalogRef:
    apiGroup: postgresql.cnpg.io
    kind: ClusterImageCatalog
    name: postgresql-main
    major: 17
  monitoring:
    enablePodMonitor: true

  # Create PVC templates on local storage for each Postgres pod
  storage:
    pvcTemplate:
      accessModes:
        - ReadWriteOnce
      resources:
        requests:
          storage: 50Gi
      storageClassName: local-path
      volumeMode: Filesystem

  # Import existing database
  bootstrap:
    recovery:
      source: pg-sonarr-data

  # Backup strategy
  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: ceph-local

  # WAL backup clusters
  externalClusters:
    - name: pg-sonarr-data
      plugin:
        name: barman-cloud.cloudnative-pg.io
        parameters:
          barmanObjectName: ceph-local
          serverName: pg-sonarr-data

  # Force Postgres to be running each instance on separate node
  affinity:
    enablePodAntiAffinity: true
    topologyKey: kubernetes.io/hostname
    podAntiAffinityType: required
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pg-sonarr-data-local-schedule
spec:
  schedule: "0 25 0 * * *" # For local store - backup every hour
  backupOwnerReference: self
  cluster:
    name: pg-sonarr-data
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
